apiVersion: apps/v1
kind: Deployment
metadata:
  name: notes-db
  labels:
    app: notes
    role: db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notes
      role: db
  template:
    metadata:
      labels:
        app: notes
        role: db
    spec:
      containers:
      - name: notes-db
        image: 2002-notes-db:rewrite
        ports:
        - containerPort: 5432
